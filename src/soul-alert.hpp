#include <iostream>

#include <pin.H>
#include <tagmap.h>

void PIN_FAST_ANALYSIS_CALL alert(ADDRINT, ADDRINT);
ADDRINT PIN_FAST_ANALYSIS_CALL assert_clean_ptr(ADDRINT, ADDRINT);
ADDRINT PIN_FAST_ANALYSIS_CALL assert_clean(ADDRINT, ADDRINT);
